import { themes } from "@/themes/themes";
import { StyleSheet } from "react-native";

export const filterSection = StyleSheet.create({
  search: {
    color: themes.light.icon.primary,
    fontSize: 18,
  },
});
