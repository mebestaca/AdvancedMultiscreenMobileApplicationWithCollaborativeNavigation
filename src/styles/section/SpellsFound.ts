import { StyleSheet } from "react-native";

export const spellsFoundSection = StyleSheet.create({
  text: {
    fontSize: 18,
    color: "white",
    padding: 10,
  },
  arrangement: {
    alignItems: "center",
    backgroundColor: "red",
  },
});
